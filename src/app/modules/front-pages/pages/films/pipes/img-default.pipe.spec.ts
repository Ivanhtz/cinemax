import { ImgDefaultPipe } from './img-default.pipe';

describe('ImgDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
